# 🧪 RFM-анализ клиентской базы аптечной сети

> **Цель**: Сегментировать клиентов по RFM-модели, чтобы персонализировать SMS-рассылки и максимизировать отдачу от маркетингового бюджета перед сезонным спадом выручки.

---

## 🎯 Задача

Сейчас у аптеки есть механизм SMS-рассылок, но он используется **неэффективно** — рассылка идёт всем подряд.  
Наша задача — **выявить ключевые группы клиентов** и предложить **персонализированные предложения** для каждой, чтобы:
- ✅ Удержать лояльных (A-класс)
- ✅ Вернуть спящих (A/B-класс)
- ✅ Повысить чек у активных (B/C-класс)
- ✅ Превратить одноразовых в постоянных (C-класс)

---

## 📊 Данные

Используется таблица `bonuscheques` из БД `apteka`.

### Структура:
| Поле              | Описание |
|-------------------|----------|
| `datetime`        | дата и время транзакции |
| `shop`            | название аптеки |
| `card`            | номер бонусной карты *(если offline — зашифрованная строка)* |
| `bonus_earned`    | начисленные бонусы |
| `bonus_spent`     | потраченные бонусы |
| `summ`            | сумма чека |
| `summ_with_disc`  | сумма с учётом скидок и бонусов |
| `doc_id`          | номер документа |

---

## 📈 Методология: RFM-анализ

RFM — модель сегментации клиентов по трём параметрам:

- **R (Recency)** — насколько давно был последний заказ
- **F (Frequency)** — как часто покупает
- **M (Monetary)** — сколько тратит
- 
### Этапы анализа:
1. **Подготовка данных**:
   - Фильтрация: только транзакции с валидными `card`
   - Агрегация по клиенту: `max(datetime)`, `count(*)`, `sum(summ_with_disc)`
2. **Расчёт RFM-метрик**:
   - Recency → количество дней с последней покупки
   - Frequency → количество покупок
   - Monetary → общая сумма
3. **Сегментация**:
   - В классических описаниях RFM часто используется 3-уровневая градация (High / Medium / Low),
   - выбор 5 уровней обусловлен следующими соображениями:
   - 1)объем выборки больше 6000 клиентов, позволяет проводить более тонкую сегментацию
   - 2)5-балльная шкала даёт 125 уникальных RFM-кодов (5×5×5), что позволяет выявлять узкие, но ценные подгруппы (например, «активные, но мало тратящие»)
   - 3)5-уровневая RFM-шкала широко применяется в ритейле, e-commerce и финансовых сервисах при работе с базами от нескольких тысяч клиентов и выше. Это де-факто отраслевой стандарт для бизнесов, стремящихся к персонализации и повышению LTV.
4. **ABC-анализ**:
   - Группировка по общей ценности (Monetary) → A (топ 20%), B (следующие 30%), C (остальные 50%)
